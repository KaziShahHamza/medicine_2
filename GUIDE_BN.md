# প্রজেক্ট গাইড (বাংলা) — MediSync

এই ডকুমেন্টটি এমনভাবে লেখা যে একজন নন-টেকনিক্যাল ছাত্র/শিক্ষকও সহজে বুঝতে পারেন—অ্যাপের কোন অংশ কোথায় আছে এবং কীভাবে কাজ করে। ভিভা/প্রেজেন্টেশনে শিক্ষক যদি কোড সম্পর্কে প্রশ্ন করেন, আপনি এখানে দেখিয়ে বুঝাতে পারবেন।

## অ্যাপের সারসংক্ষেপ
- উদ্দেশ্য: ওষুধ ম্যানেজমেন্ট, রিমাইন্ডার, এবং স্বাস্থ্য ডেটা লগ ও চার্টে দেখা।
- ফ্রন্টএন্ড: React + Vite (আধুনিক, দ্রুত ডেভ সার্ভার)।
- ব্যাকএন্ড: Node.js + Express (API সার্ভার) + MongoDB (ডাটাবেস)।
- অথেন্টিকেশন: JWT টোকেন দিয়ে লগইন/লগআউট ও প্রটেক্টেড রুট।

## কীভাবে ব্যাখ্যা করবেন (টিচারকে)
- "ফ্রন্টএন্ড" মানে ব্রাউজারে দেখা অংশ—পেজ, ফর্ম, চার্ট।
- "ব্যাকএন্ড" মানে সার্ভার—ডাটাবেসে ডেটা রাখা/নেওয়া, সিকিউরিটি চেক।
- ইউজার কিছু করলে (যেমন লগইন/ফর্ম সাবমিট), ফ্রন্টএন্ড থেকে ব্যাকএন্ডে API কল যায়; ব্যাকএন্ড যাচাই করে ডাটাবেসে কাজ করে, রেসপন্স ফেরত পাঠায়; ফ্রন্টএন্ড তা দেখায়।

## রান/ডেমো (সংক্ষেপ)
- সার্ভার চালু: `server` ফোল্ডারে গিয়ে `npm run dev`।
- ক্লায়েন্ট চালু: `client` ফোল্ডারে গিয়ে `npm run dev`।
- ব্রাউজারে অ্যাপ: `http://localhost:5173`।
- সার্ভার API: `http://localhost:5000`।

## ফোল্ডার ম্যাপ ও গুরুত্বপূর্ণ ফাইল

### ফ্রন্টএন্ড (client)
- মূল স্টার্ট পয়েন্ট: [client/src/main.jsx](client/src/main.jsx)
- অ্যাপ কম্পোনেন্ট: [client/src/App.jsx](client/src/App.jsx)
- পেজসমূহ: [client/src/pages](client/src/pages)
  - উদাহরণ: [client/src/pages/Login.jsx](client/src/pages/Login.jsx), [client/src/pages/Signup.jsx](client/src/pages/Signup.jsx), [client/src/pages/Dashboard.jsx](client/src/pages/Dashboard.jsx), [client/src/pages/Health.jsx](client/src/pages/Health.jsx)
- কম্পোনেন্টস: [client/src/components](client/src/components)
  - উদাহরণ: [client/src/components/MedicineForm.jsx](client/src/components/MedicineForm.jsx), [client/src/components/MedicineList.jsx](client/src/components/MedicineList.jsx), [client/src/components/HealthLogForm.jsx](client/src/components/HealthLogForm.jsx), [client/src/components/HealthCharts.jsx](client/src/components/HealthCharts.jsx)
  - প্রটেক্টেড রুট: [client/src/components/ProtectedRoute.jsx](client/src/components/ProtectedRoute.jsx) — লগইন ছাড়া কিছু পেজে ঢুকতে দেয় না।
- কনটেক্সট (গ্লোবাল স্টেট): [client/src/context](client/src/context)
  - লগইন/লগআউট: [client/src/context/AuthContext.jsx](client/src/context/AuthContext.jsx) — এখানে ইউজার তথ্য ও টোকেন রাখা/মুছার লজিক আছে।
  - মেডিসিন স্টেট: [client/src/context/MedicineContext.jsx](client/src/context/MedicineContext.jsx) — ওষুধের তালিকা ম্যানেজ।
- হুকস: [client/src/hooks](client/src/hooks)
  - হেলথ লগ হুক: [client/src/hooks/useHealthLogs.js](client/src/hooks/useHealthLogs.js)
  - রিমাইন্ডার হুক: [client/src/hooks/useMedicineReminder.js](client/src/hooks/useMedicineReminder.js)
- ইউটিলস: [client/src/utils/timeMap.js](client/src/utils/timeMap.js) — সময়/ফ্রিকোয়েন্সি ম্যাপিং।

ফ্রন্টএন্ড কীভাবে কাজ করে (সাধারণ ভাষায়):
- ইউজার লগইন করলে `AuthContext` টোকেন ও ইউজার তথ্য `localStorage`-এ রাখে, যাতে রিফ্রেশের পরও লগইন মনে থাকে। দেখুন: [client/src/context/AuthContext.jsx](client/src/context/AuthContext.jsx)
- কোন পেজ প্রটেক্টেড হলে `ProtectedRoute` চেক করে ইউজার আছে কি না; না থাকলে লগইন পেজে পাঠায়। দেখুন: [client/src/components/ProtectedRoute.jsx](client/src/components/ProtectedRoute.jsx)
- ওষুধ যোগ/এডিট/ডিলিট—ফর্ম সাবমিট করলে API তে কল যায়; রেসপন্স এলে লিস্ট আপডেট হয়।
- হেলথ লগ করলে চার্ট আপডেট হয়—`Chart.js` দিয়ে গ্রাফ দেখায়। দেখুন: [client/src/components/HealthCharts.jsx](client/src/components/HealthCharts.jsx)

### ব্যাকএন্ড (server)
- সার্ভার এন্ট্রি: [server/server.js](server/server.js) — এখানে এক্সপ্রেস অ্যাপ, MongoDB কানেকশন, এবং রাউট রেজিস্টার করা আছে।
- মিডলওয়্যার (সিকিউরিটি): [server/middleware/auth.js](server/middleware/auth.js)
  - কাজ: রিকোয়েস্ট হেডার থেকে JWT টোকেন নিয়ে যাচাই করা; ঠিক হলে `req.userId` সেট করা, নাহলে 401।
- রাউটস (API): [server/routes](server/routes)
  - অথেন্টিকেশন: [server/routes/auth.routes.js](server/routes/auth.routes.js)
    - `POST /api/auth/signup`: নতুন ইউজার — পাসওয়ার্ড হ্যাশ করে সেভ।
    - `POST /api/auth/login`: লগইন — ঠিক হলে JWT টোকেন তৈরি করে ফেরত দেয়।
  - মেডিসিন: [server/routes/medicine.routes.js](server/routes/medicine.routes.js)
    - `GET /api/medicines`: ইউজারের সব মেডিসিন।
    - `POST /api/medicines`: নতুন মেডিসিন তৈরি।
    - `PUT /api/medicines/:id`: আপডেট।
    - `DELETE /api/medicines/:id`: ডিলিট।
  - হেলথ লগ: [server/routes/health.routes.js](server/routes/health.routes.js)
    - `POST /api/health`: নতুন লগ তৈরি।
    - `GET /api/health`: ইউজারের সব লগ।
    - `DELETE /api/health/:id`: লগ ডিলিট।
- মডেলস (ডাটাবেস স্কিমা): [server/models](server/models)
  - ইউজার: [server/models/User.js](server/models/User.js)
  - মেডিসিন: [server/models/Medicine.js](server/models/Medicine.js)
  - হেলথ লগ: [server/models/HealthLog.js](server/models/HealthLog.js)
- রিমাইন্ডার সার্ভিস: [server/services/reminderScheduler.js](server/services/reminderScheduler.js) — নির্দিষ্ট সময়ে রিমাইন্ডার শিডিউল করতে `node-cron` ব্যবহার।

ব্যাকএন্ড কীভাবে কাজ করে (সাধারণ ভাষায়):
- ফ্রন্টএন্ড যখন API কল করে, ব্যাকএন্ড প্রথমে `auth` মিডলওয়্যার দিয়ে টোকেন যাচাই করে (যদি প্রটেক্টেড রুট হয়)।
- তারপর সংশ্লিষ্ট রাউট (যেমন মেডিসিন/হেলথ) মডেল দিয়ে MongoDB তে ডেটা পড়ে/লিখে।
- সব ঠিক থাকলে JSON ডেটা রেসপন্স হিসেবে ফিরে যায়, ফ্রন্টএন্ড তা রেন্ডার করে।

## উদাহরণ-ভিত্তিক ব্যাখ্যা (Q&A স্টাইল)

1) প্রশ্ন: "লগইন কীভাবে কাজ করে?"
- ফ্রন্টএন্ডের লগইন ফর্ম সাবমিট হলে `POST /api/auth/login` এ ইমেইল/পাসওয়ার্ড যায়।
- ব্যাকএন্ড ইউজার খুঁজে পায়, পাসওয়ার্ড মিলিয়ে দেখে; ঠিক থাকলে JWT টোকেন বানায়। দেখুন: [server/routes/auth.routes.js](server/routes/auth.routes.js)
- ফ্রন্টএন্ড `AuthContext` টোকেন ও ইউজার তথ্য `localStorage`-এ রাখে। দেখুন: [client/src/context/AuthContext.jsx](client/src/context/AuthContext.jsx)
- এরপর প্রটেক্টেড পেজে ঢোকা যায়, কারণ `ProtectedRoute` ইউজার আছে কিনা দেখে নেয়। দেখুন: [client/src/components/ProtectedRoute.jsx](client/src/components/ProtectedRoute.jsx)

2) প্রশ্ন: "প্রটেক্টেড API কিভাবে সিকিউর করা?"
- ব্যাকএন্ডে `auth` মিডলওয়্যার আছে—হেডার থেকে টোকেন পড়ে যাচাই করে, নাহলে 401। দেখুন: [server/middleware/auth.js](server/middleware/auth.js)
- মেডিসিন/হেলথ রাউটগুলোতে এই মিডলওয়্যার ব্যবহার করা হয়েছে—তাই টোকেন ছাড়া ডেটা পাওয়া যায় না। দেখুন: [server/routes/medicine.routes.js](server/routes/medicine.routes.js), [server/routes/health.routes.js](server/routes/health.routes.js)

3) প্রশ্ন: "ওষুধ যোগ করলে কী হয়?"
- ফ্রন্টএন্ডে ফর্ম থেকে তথ্য নিয়ে `POST /api/medicines` কল হয়।
- ব্যাকএন্ড টোকেন যাচাই করে, তারপর MongoDB তে নতুন ডকুমেন্ট সেভ হয় (ইউজার আইডি সহ)।
- রেসপন্স এলে ফ্রন্টএন্ড লিস্ট আপডেট হয়—ইউজার নতুন আইটেম দেখতে পায়।

4) প্রশ্ন: "হেলথ চার্ট কীভাবে আপডেট হয়?"
- আপনি নতুন হেলথ লগ দিলে `POST /api/health` এ সেভ হয়।
- এরপর `GET /api/health` থেকে ডেটা এনে `Chart.js` দিয়ে চার্ট রেন্ডার হয়। দেখুন: [client/src/components/HealthCharts.jsx](client/src/components/HealthCharts.jsx)

5) প্রশ্ন: "রিমাইন্ডার কীভাবে চলে?"
- ব্যাকএন্ডে `node-cron` দিয়ে সময়ভিত্তিক টাস্ক শিডিউল করা যায়।
- এখানে `reminderScheduler.js` ফাইলে লজিক থাকে—নির্দিষ্ট সময়ে চেক করে কার কোন মেডিসিনের রিমাইন্ডার ট্রিগার হবে (নোটিফিকেশন/ইমেইল কনফিগার করলে সেখানে পাঠানো যায়)। দেখুন: [server/services/reminderScheduler.js](server/services/reminderScheduler.js)

6) প্রশ্ন: "ডাটাবেস কোথায় কানেক্ট করা?"
- `server.js` এ MongoDB কানেকশন করা আছে: `mongodb://localhost:27017/medisync2`। দেখুন: [server/server.js](server/server.js)

## শিক্ষককে দেখানোর জন্য দ্রুত স্ক্রিপ্ট
- "এটা ফ্রন্টএন্ড ফোল্ডার (client), যেখানে পেজ/কম্পোনেন্ট আছে।"
- "এটা ব্যাকএন্ড ফোল্ডার (server), যেখানে API রাউট, মিডলওয়্যার ও মডেল আছে।"
- "লগইন হলে টোকেন সেভ হয়—এই ফাইলে (AuthContext)।"
- "প্রটেক্টেড রুটে ঢুকতে `ProtectedRoute` চেক করে।"
- "মেডিসিন/হেলথ API এখানে—রাউট ফাইলগুলোতে।"
- "চার্ট এখানে—HealthCharts।"
- "রিমাইন্ডার শিডিউলার এখানে—reminderScheduler।"

## ছোট্ট টিপস
- ফাইল নাম/লোকেশন দেখাতে উপরের লিঙ্কে ক্লিক করুন—এডিটরে সরাসরি ফাইল খুলবে।
- কোন অংশ বুঝতে না পারলে—রাউট (API) আর সংশ্লিষ্ট কম্পোনেন্ট/কনটেক্সট একসাথে দেখে নিলে দ্রুত বোঝা যায়।

---

এই গাইডটি দিয়ে আপনি সহজে বুঝাতে পারবেন—অ্যাপের কোন অংশ কোথায় এবং কী কাজ করে। শুভেচ্ছা! 🎓💊📊
